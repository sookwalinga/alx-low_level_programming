#include "main.h"
#include <stdio.h>

/**
 * mul - multiply two integers
 *
 * @a: first number
 * @b: second number
 *
 * Return: the product of the two integers
 */
int mul(int a, int b)
{
return (a * b);
}
